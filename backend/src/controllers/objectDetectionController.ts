// src/controllers/objectDetectionController.ts
import { Request, Response } from 'express';
// import objectDetectionService from '../services/objectDetectionService';

const detectObjects = (req: Request, res: Response) => {
  // Handle object detection logic using objectDetectionService
};

export default {
  detectObjects,
};
